for each in range(len(let6)):
		for each2 in range(len(let1)):
			for each3 in range(len(let2)):
				for each4 in range(len(let1)):
					pass14.write(let6[each] + let1[each2] + let2[each3] + let1[each4])
					pass14.write("\n")
	for each in range(len(let6)):
		for each2 in range(len(let1)):
			for each3 in range(len(let1)):
				for each4 in range(len(let2)):
					pass14.write(let6[each] + let1[each2] + let1[each3] + let2[each4])
					pass14.write("\n")
	for each in range(len(let2)):
		for each2 in range(len(let1)):
			for each3 in range(len(let6)):
				for each4 in range(len(let1)):
					pass14.write(let2[each] + let1[each2] + let6[each3] + let1[each4])
					pass14.write("\n")
	for each in range(len(let2)):
		for each2 in range(len(let1)):
			for each3 in range(len(let1)):
				for each4 in range(len(let6)):
					pass14.write(let2[each] + let1[each2] + let1[each3] + let6[each4])
					pass14.write("\n")
	for each in range(len(let2)):
		for each2 in range(len(let6)):
			for each3 in range(len(let1)):
				for each4 in range(len(let1)):
					pass14.write(let2[each] + let6[each2] + let1[each3] + let1[each4])
					pass14.write("\n")
	for each in range(len(let1)):
		for each2 in range(len(let2)):
			for each3 in range(len(let6)):
				for each4 in range(len(let1)):
					pass14.write(let1[each] + let2[each2] + let6[each3] + let1[each4])
					pass14.write("\n")
	for each in range(len(let2)):
		for each2 in range(len(let1)):
			for each3 in range(len(let6)):
				for each4 in range(len(let1)):
					pass14.write(let2[each] + let1[each2] + let6[each3] + let1[each4])
					pass14.write("\n")					
	for each in range(len(let1)):
		for each2 in range(len(let6)):
			for each3 in range(len(let2)):
				for each4 in range(len(let1)):
					pass14.write(let1[each] + let6[each2] + let2[each3] + let1[each4])
					pass14.write("\n")
	for each in range(len(let1)):
		for each2 in range(len(let2)):
			for each3 in range(len(let1)):
				for each4 in range(len(let6)):
					pass14.write(let1[each] + let2[each2] + let1[each3] + let6[each4])
					pass14.write("\n")
	for each in range(len(let1)):
		for each2 in range(len(let6)):
			for each3 in range(len(let1)):
				for each4 in range(len(let2)):
					pass14.write(let1[each] + let6[each2] + let1[each3] + let2[each4])
					pass14.write("\n")
	for each in range(len(let1)):
		for each2 in range(len(let1)):
			for each3 in range(len(let2)):
				for each4 in range(len(let6)):
					pass14.write(let1[each] + let1[each2] + let2[each3] + let6[each4])
					pass14.write("\n")
	for each in range(len(let1)):
		for each2 in range(len(let1)):
			for each3 in range(len(let6)):
				for each4 in range(len(let2)):
					pass14.write(let1[each] + let1[each2] + let6[each3] + let2[each4])
					pass14.write("\n")				
	for each in range(len(let1)):
		for each2 in range(len(let1)):
			for each3 in range(len(let1)):
				for each4 in range(len(let1)):
					for each5 in range(len(let6)):
						pass14.write(let1[each] + let1[each2] + let1[each3] + let1[each4] + let6[each5])
						pass14.write("\n")
						
			for each in range(len(let6)):
		for each2 in range(len(let2)):
			for each3 in range(len(let1)):
				for each4 in range(len(let1)):
					pass14.write(let6[each] + let2[each2] + let1[each3] + let1[each4])
					pass14.write("\n")
	
	for each in range(len(let1)):
		for each2 in range(len(let6)):
			for each3 in range(len(let1)):
				for each4 in range(len(let1)):
					for each5 in range(len(let1)):
						pass14.write(let1[each] + let6[each2] + let1[each3] + let1[each4] + let1[each5])
						pass14.write("\n")
	for each in range(len(let1)):
		for each2 in range(len(let1)):
			for each3 in range(len(let6)):
				for each4 in range(len(let1)):
					for each5 in range(len(let1)):
						pass14.write(let1[each] + let1[each2] + let6[each3] + let1[each4] + let1[each5])
						pass14.write("\n")
	for each in range(len(let1)):
		for each2 in range(len(let1)):
			for each3 in range(len(let1)):
				for each4 in range(len(let6)):
					for each5 in range(len(let1)):
						pass14.write(let1[each] + let1[each2] + let1[each3] + let6[each4] + let1[each5])
						pass14.write("\n")
	for each in range(len(let6)):
		for each2 in range(len(let1)):
			for each3 in range(len(let1)):
				for each4 in range(len(let1)):
					for each5 in range(len(let1)):
						pass14.write(let6[each] + let1[each2] + let1[each3] + let1[each4] + let1[each5])
						pass14.write("\n")
						
						
						
					---\
for each in range(len(let5)):
		for each2 in range(len(let2)):
			for each3 in range(len(let1)):
				for each4 in range(len(let2)):
					for each5 in range(len(let1)):
						pass15.write(let5[each] + let1[each2] + let1[each3] + let2[each4] + let1[each5])
						pass15.write("\n")					
for each in range(len(let5)):
		for each2 in range(len(let3)):
			for each3 in range(len(let1)):
				for each4 in range(len(let1)):
					pass15.write(let5[each] + let3[each2] + let1[each3] + let1[each4])
					pass15.write("\n")
	for each in range(len(let5)):
		for each2 in range(len(let1)):
			for each3 in range(len(let3)):
				for each4 in range(len(let1)):
					pass15.write(let5[each] + let1[each2] + let3[each3] + let1[each4])
					pass15.write("\n")	
	for each in range(len(let5)):
		for each2 in range(len(let1)):
			for each3 in range(len(let1)):
				for each4 in range(len(let3)):
					pass15.write(let5[each] + let1[each2] + let1[each3] + let3[each4])
					pass15.write("\n")
	for each in range(len(let3)):
		for each2 in range(len(let5)):
			for each3 in range(len(let1)):
				for each4 in range(len(let1)):
					pass15.write(let3[each] + let5[each2] + let1[each3] + let1[each4])
					pass15.write("\n")									
	for each in range(len(let3)):
		for each2 in range(len(let1)):
			for each3 in range(len(let5)):
				for each4 in range(len(let1)):
					pass15.write(let3[each] + let1[each2] + let5[each3] + let1[each4])
					pass15.write("\n")
	for each in range(len(let3)):
		for each2 in range(len(let1)):
			for each3 in range(len(let1)):
				for each4 in range(len(let5)):
					pass15.write(let3[each] + let1[each2] + let1[each3] + let5[each4])
					pass15.write("\n")	
	for each in range(len(let1)):
		for each2 in range(len(let1)):
			for each3 in range(len(let3)):
				for each4 in range(len(let5)):
					pass15.write(let1[each] + let1[each2] + let3[each3] + let5[each4])
					pass15.write("\n")
	for each in range(len(let1)):
		for each2 in range(len(let1)):
			for each3 in range(len(let5)):
				for each4 in range(len(let3)):
					pass15.write(let1[each] + let1[each2] + let5[each3] + let3[each4])
					pass15.write("\n")		
	for each in range(len(let1)):
		for each2 in range(len(let3)):
			for each3 in range(len(let1)):
				for each4 in range(len(let5)):
					pass15.write(let1[each] + let3[each2] + let1[each3] + let5[each4])
					pass15.write("\n")
	for each in range(len(let1)):
		for each2 in range(len(let3)):
			for each3 in range(len(let5)):
				for each4 in range(len(let1)):
					pass15.write(let1[each] + let3[each2] + let5[each3] + let1[each4])
					pass15.write("\n")	
	for each in range(len(let1)):
		for each2 in range(len(let5)):
			for each3 in range(len(let3)):
				for each4 in range(len(let1)):
					pass15.write(let1[each] + let5[each2] + let3[each3] + let1[each4])
					pass15.write("\n")
	for each in range(len(let1)):
		for each2 in range(len(let5)):
			for each3 in range(len(let1)):
				for each4 in range(len(let3)):
					pass15.write(let1[each] + let5[each2] + let1[each3] + let3[each4])
					pass15.write("\n")			
	for each in range(len(let5)):
		for each2 in range(len(let2)):
			for each3 in range(len(let1)):
				for each4 in range(len(let1)):
					for each5 in range(len(let1)):
						pass15.write(let5[each] + let2[each2] + let1[each3] + let1[each4] + let1[each5])
						pass15.write("\n")
	for each in range(len(let5)):
		for each2 in range(len(let1)):
			for each3 in range(len(let2)):
				for each4 in range(len(let1)):
					for each5 in range(len(let1)):
						pass15.write(let5[each] + let1[each2] + let2[each3] + let1[each4] + let1[each5])
						pass15.write("\n")								

===
for each in range(len(let5)):
		for each2 in range(len(let2)):
			for each3 in range(len(let1)):
				for each4 in range(len(let1)):
					pass8.write(let5[each] + let2[each2] + let1[each3] + let1[each4])
					pass8.write("\n")
	for each in range(len(let5)):
		for each2 in range(len(let1)):
			for each3 in range(len(let2)):
				for each4 in range(len(let1)):
					pass8.write(let5[each] + let1[each2] + let2[each3] + let1[each4])
					pass8.write("\n")
	for each in range(len(let5)):
		for each2 in range(len(let1)):
			for each3 in range(len(let1)):
				for each4 in range(len(let2)):
					pass8.write(let5[each] + let1[each2] + let1[each3] + let2[each4])
					pass8.write("\n")
	for each in range(len(let1)):
		for each2 in range(len(let5)):
			for each3 in range(len(let2)):
				for each4 in range(len(let1)):
					pass8.write(let1[each] + let5[each2] + let2[each3] + let1[each4])
					pass8.write("\n")
	for each in range(len(let1)):
		for each2 in range(len(let2)):
			for each3 in range(len(let5)):
				for each4 in range(len(let1)):
					pass8.write(let1[each] + let2[each2] + let5[each3] + let1[each4])
					pass8.write("\n")
					
					for each in range(len(let2)):
		for each2 in range(len(let3)):
			for each3 in range(len(let3)):
				for each4 in range(len(let1)):
					pass9.write(let2[each] + let3[each2] + let3[each3] + let1[each4])
					pass9.write("\n")
	for each in range(len(let4)):
		for each2 in range(len(let2)):
			for each3 in range(len(let2)):
				for each4 in range(len(let1)):
					pass9.write(let4[each] + let2[each2] + let2[each3] + let1[each4])
					pass9.write("\n")
	for each in range(len(let2)):
		for each2 in range(len(let3)):
			for each3 in range(len(let4)):
				pass9.write(let2[each] + let3[each2] + let4[each3])
				pass9.write("\n")


	for each in range(len(let5)):
		for each2 in range(len(let2)):
			for each3 in range(len(let1)):
				for each4 in range(len(let1)):
					pass11.write(let5[each].capitalize() + let2[each2] + let1[each3] + let1[each4])
					pass11.write("\n")
	for each in range(len(let5)):
		for each2 in range(len(let1)):
			for each3 in range(len(let2)):
				for each4 in range(len(let1)):
					pass11.write(let5[each].capitalize() + let1[each2] + let2[each3] + let1[each4])
					pass11.write("\n")
	for each in range(len(let5)):
		for each2 in range(len(let1)):
			for each3 in range(len(let1)):
				for each4 in range(len(let2)):
					pass11.write(let5[each].capitalize() + let1[each2] + let1[each3] + let2[each4])
					pass11.write("\n")
	for each in range(len(let1)):
		for each2 in range(len(let5)):
			for each3 in range(len(let2)):
				for each4 in range(len(let1)):
					pass11.write(let1[each].capitalize() + let5[each2] + let2[each3] + let1[each4])
					pass11.write("\n")
	for each in range(len(let1)):
		for each2 in range(len(let2)):
			for each3 in range(len(let5)):
				for each4 in range(len(let1)):
					pass11.write(let1[each].capitalize() + let2[each2] + let5[each3] + let1[each4])
					pass11.write("\n")
for each in range(len(let2)):
		for each2 in range(len(let3)):
			for each3 in range(len(let3)):
				for each4 in range(len(let1)):
					pass12.write(let2[each].capitalize() + let3[each2] + let3[each3] + let1[each4])
					pass12.write("\n")
	for each in range(len(let4)):
		for each2 in range(len(let2)):
			for each3 in range(len(let2)):
				for each4 in range(len(let1)):
					pass12.write(let4[each].capitalize() + let2[each2] + let2[each3] + let1[each4])
					pass12.write("\n")
					
					for each in range(len(let5)):
		for each2 in range(len(let1)):
			for each3 in range(len(let1)):
				for each4 in range(len(let1)):
					pass1.write(let5[each] + let1[each2] + let1[each3] + let1[each4])
					pass1.write("\n")
	for each in range(len(let1)):
		for each2 in range(len(let1)):
			for each3 in range(len(let1)):
				for each4 in range(len(let5)):
					pass1.write(let1[each] + let1[each2] + let1[each3] + let5[each4])
					pass1.write("\n")
	for each in range(len(let1)):
		for each2 in range(len(let5)):
			for each3 in range(len(let1)):
				for each4 in range(len(let1)):
					pass1.write(let1[each] + let5[each2] + let1[each3] + let1[each4])
					pass1.write("\n")
	for each in range(len(let1)):
		for each2 in range(len(let1)):
			for each3 in range(len(let5)):
				for each4 in range(len(let1)):
					pass1.write(let1[each] + let1[each2] + let5[each3] + let1[each4])
					pass1.write("\n")
					
			
			
			
	for each in range(len(let1)):
		for each2 in range(len(let2)):
			for each3 in range(len(let2)):
				for each4 in range(len(let3)):
					pass2.write(let1[each] + let2[each2] + let2[each3] + let3[each4])
					pass2.write("\n")
	for each in range(len(let2)):
		for each2 in range(len(let1)):
			for each3 in range(len(let2)):
				for each4 in range(len(let3)):
					pass2.write(let2[each] + let1[each2] + let2[each3] + let3[each4])
					pass2.write("\n")
	for each in range(len(let2)):
		for each2 in range(len(let2)):
			for each3 in range(len(let1)):
				for each4 in range(len(let3)):
					pass2.write(let2[each] + let2[each2] + let1[each3] + let3[each4])
					pass2.write("\n")
	for each in range(len(let2)):
		for each2 in range(len(let2)):
			for each3 in range(len(let3)):
				for each4 in range(len(let1)):
					pass2.write(let2[each] + let2[each2] + let3[each3] + let1[each4])
					pass2.write("\n")
	for each in range(len(let1)):
		for each2 in range(len(let3)):
			for each3 in range(len(let2)):
				for each4 in range(len(let2)):
					pass2.write(let1[each] + let3[each2] + let2[each3] + let2[each4])
					pass2.write("\n")
	for each in range(len(let1)):
		for each2 in range(len(let2)):
			for each3 in range(len(let3)):
				for each4 in range(len(let2)):
					pass2.write(let1[each] + let2[each2] + let3[each3] + let2[each4])
					pass2.write("\n")
	for each in range(len(let3)):
		for each2 in range(len(let1)):
			for each3 in range(len(let2)):
				for each4 in range(len(let2)):
					pass2.write(let3[each] + let1[each2] + let2[each3] + let2[each4])
					pass2.write("\n")
	for each in range(len(let3)):
		for each2 in range(len(let2)):
			for each3 in range(len(let1)):
				for each4 in range(len(let2)):
					pass2.write(let3[each] + let2[each2] + let1[each3] + let2[each4])
					pass2.write("\n")
	for each in range(len(let3)):
		for each2 in range(len(let2)):
			for each3 in range(len(let2)):
				for each4 in range(len(let1)):
					pass2.write(let3[each] + let2[each2] + let2[each3] + let1[each4])
					pass2.write("\n")
	for each in range(len(let2)):
		for each2 in range(len(let1)):
			for each3 in range(len(let3)):
				for each4 in range(len(let2)):
					pass2.write(let2[each] + let1[each2] + let3[each3] + let2[each4])
					pass2.write("\n")
	for each in range(len(let2)):
		for each2 in range(len(let3)):
			for each3 in range(len(let1)):
				for each4 in range(len(let2)):
					pass2.write(let2[each] + let3[each2] + let1[each3] + let2[each4])
					pass2.write("\n")
	for each in range(len(let2)):
		for each2 in range(len(let3)):
			for each3 in range(len(let2)):
				for each4 in range(len(let1)):
					pass2.write(let2[each] + let3[each2] + let2[each3] + let1[each4])
					pass2.write("\n")
	for each in range(len(let1)):
		for each2 in range(len(let1)):
			for each3 in range(len(let3)):
				for each4 in range(len(let3)):
					pass3.write(let1[each] + let1[each2] + let3[each3] + let3[each4])
					pass3.write("\n")
	for each in range(len(let1)):
		for each2 in range(len(let3)):
			for each3 in range(len(let1)):
				for each4 in range(len(let3)):
					pass3.write(let1[each] + let3[each2] + let1[each3] + let3[each4])
					pass3.write("\n")
	for each in range(len(let1)):
		for each2 in range(len(let3)):
			for each3 in range(len(let3)):
				for each4 in range(len(let1)):
					pass3.write(let1[each] + let3[each2] + let3[each3] + let1[each4])
					pass3.write("\n")
	for each in range(len(let3)):
		for each2 in range(len(let3)):
			for each3 in range(len(let1)):
				for each4 in range(len(let1)):
					pass3.write(let3[each] + let3[each2] + let1[each3] + let1[each4])
					pass3.write("\n")
	for each in range(len(let3)):
		for each2 in range(len(let1)):
			for each3 in range(len(let3)):
				for each4 in range(len(let1)):
					pass3.write(let3[each] + let1[each2] + let3[each3] + let1[each4])
					pass3.write("\n")
	for each in range(len(let3)):
		for each2 in range(len(let1)):
			for each3 in range(len(let1)):
				for each4 in range(len(let3)):
					pass3.write(let3[each] + let1[each2] + let1[each3] + let3[each4])
					pass3.write("\n")
